import { DashboardHeader } from "../components/DashboardHeader";

const Dashboard = () => {
  return (
    <main className=" container mx-auto mt-[3.90rem] mb-2 h-[81dvh] flex flex-col  ">
      <DashboardHeader />
    </main>
  );
};

export default Dashboard;
